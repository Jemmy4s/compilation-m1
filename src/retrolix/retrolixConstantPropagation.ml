(** This module implements the constant propagation optimization. *)

(** [reachable_definitions cfg p] determines the set of definitions
    that reaches each point of the program [p]. *)
let reachable_definitions cfg p =
     failwith "Students! This is your work!"
     </sujet. *)

(** [simplify cfg p] optimises [p] by propagating constants and
    performing as much computation as possible statically using
    this propagation. *)
let simplify cfg p =
     failwith "Students! This is your work!"
     </sujet. *)
